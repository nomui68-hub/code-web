# V11.9

- Sửa lỗi hệ `\heva`, `\hoac` còn hiện `\left`, `\right` hoặc mất ngoặc.
- Không đổi `\{`, `\}` trong công thức toán ở trình duyệt.
- Dọn `\break`, `\newline`, `\linebreak` tốt hơn.
- Khuyến nghị xóa thư mục đề cũ trong `exams/<mã đề>` và `images/<mã đề>` rồi tạo lại đề.
